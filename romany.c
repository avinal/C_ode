#include<stdio.h>
#include<string.h>
char roman(int);
int main()
{
    int year;
    char rom[30];
    printf("Enter a year.");
    scanf("%d",&year);
}
char roman(int year)
{
    int digit, i;
    char rom[30], rodi[7]="IVXLCDM";
    for(i=1; i<=4; i++)
    {
        digit=year%10;
        if(digit<5)
        {
            for()
        }
    }
    

}