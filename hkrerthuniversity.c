#include<stdio.h>
#include<malloc.h>

struct hkrerthuniversity
{
    char name[8];
    struct hkrerthuniversity *next;
    struct student
    {
        
    };
    
};
typedef struct hkrerthuniversity identity;
identity *class=NULL;
identity *student=NULL;
identity createlist(identity *);
identity display(identity *);

int main(int argc, char const *argv[])
{
    
    return 0;
}

