#include<stdio.h>
#include<malloc.h>
struct partition
{
    int data;
    struct partition *next;
};
typedef struct partition node;
int main(int argc, char const *argv[])
{
    int in_number;
    scanf("%d",&in_number);
    
    return 0;
}
